# URL base do backend Nest (com /api no final). Em dev o padrão é localhost.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Produção (Vercel): opcional — se vazio, o código usa a API padrão do projeto (Railway).
# NEXT_PUBLIC_API_URL=https://liga7esports-api.railway.app/api

# CORS: no Railway, FRONTEND_URL deve ser a origem do site (ex.: https://liga7esports-frontend.vercel.app).
