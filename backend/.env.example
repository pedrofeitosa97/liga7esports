DATABASE_URL="postgresql://postgres:password@localhost:5432/liga7esports?schema=public"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"
PORT=3001
NODE_ENV=development
# Origem do frontend (CORS). Várias URLs: separar por vírgula. Barra final é opcional.
# Local:
FRONTEND_URL="http://localhost:3000"
# Produção (Railway): copie este valor nas variáveis do serviço da API
# FRONTEND_URL="https://liga7esports-frontend.vercel.app"
# Opcional: lista explícita (sobrescreve FRONTEND_URL se definido)
# CORS_ORIGINS="https://seu-app.vercel.app,https://outro.dominio.com"
# Preview deployments *.vercel.app (defina true no Railway/Vercel)
# CORS_ALLOW_VERCEL_PREVIEWS="true"
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE=5242880
